# Claude API Configuration
# Get your API key from https://console.anthropic.com
VITE_CLAUDE_API_KEY=

# Note: For production, API keys should NEVER be stored in environment variables
# accessible to the browser. Instead, use a backend proxy to handle API calls.
# This demo uses dangerouslyAllowBrowser for demonstration purposes only.
